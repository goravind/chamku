# Backup folder

CSV backups are synced here when you use **Settings → Backup → Sync to GitHub**.

The file `relocation-tasks.csv` is created/updated in this folder when you sync from the app. Commit and push to save your backup to the repo.
